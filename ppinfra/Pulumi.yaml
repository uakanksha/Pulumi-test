name: lab08
runtime: yaml
description: A static website on AWS S3 and CloudFront using Pulumi

resources:
  my-bucket:
    type: aws:s3:BucketV2

outputs:
  bucketName: ${my-bucket.id}

config:
  pulumi:tags:
    value:
      pulumi:template: aws-yaml
